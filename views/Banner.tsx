import { Section } from "@/components";

const Banner = () => {
  return (
    <Section id="banner">
      <div className="flex items-center justify-center w-full h-screen bg-no-repeat bg-cover bg-cpu">
        <h1>Hello asads</h1>
      </div>
    </Section>
  );
};

export default Banner;
